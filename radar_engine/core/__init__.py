"""
radar_engine.core — Shared typed contracts and base interfaces.

Modules:
    enums      — All semantic label enumerations (ValidityState, PostureLabel, …)
    models     — Typed dataclasses for all engine inputs and outputs
    context    — RadarContext: shared pipeline state passed between modules
    base       — RadarModule ABC: the common interface for all pipeline stages
"""
